#!/bin/env goo

use tree from animation_tree;
use game;

tree.animation {
  name = "Idle",
  on_update = fn {
    if abs(game.player.velocity.x) > 0.1 {
      tree.switch_to "Running";
    }
  }
};

tree.animation {
  name = "Idle",
  on_update = fn {
    if abs(game.player.velocity.x) > 0.1 {
      tree.switch_to "Running";
    }
  }
};


let a = {
}
