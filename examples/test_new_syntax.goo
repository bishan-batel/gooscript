
fn main() {
  let a = 0;
}


let integers = [1, 2, 3, 4, 5, 6, 7]

let integers = [
  1 
  2 
  3 
  4 
  5 
  6 
  7
]



let main = fn(x) => x * 2;

let a = [10, 420, 4].map(fn(x) => x * 2);

# Type Syntax for better static-analysis
type Player {
  velocity: Vec2,
  position: Vec2,
  speed: f32,
};


# Soon to be illegal syntax, name will only be allowed for statement-level functions due to ambiguaty
let some_function = fn some_function() {
};

# Legal 
let some_function = fn() {};


# The new way of defining a function such as that is:
fn some_function() {
}

let a = fn() { };


method get() { }

impl Player {
  fn new() {
    return {
      velocity: Vec2.zero,
      position: Vec2.zero,
      speed: 10
    };
  }
}
